---
layout: layouts/base.njk
---

<div class="fixed t-0 l-0 w-100p z-n1 bg-hero-short">
  <div class="h-26">

  </div>
  <div class="h-20">

  </div>
</div>

<a href="#nav" class="flex bg-white shadow-2 color-black align-items-center justify-content-center px-5 h-11 radius-round fixed b-0 r-0 mb-6 mr-6 font-400 font-sm">
  Back to top
</a>


<section>
  {% include "partials/nav.njk" %}
  <div class="wrapper">
    <div class="pt-15 pb-18">
      <h1 class="font-6xl font-300 mb-6 tracking-n1 color-white">
        {{ title }}
      </h1>

      <p class="max-w-26 font-lg font-400 color-white opacity-70">
        A simple and useful list of all available CSS properties and variants.
      </p>
    </div>
  </div>
</section>

<section>
	<div class="wrapper">
		<div class="grid grid-cols-12 bg-white radius-md shadow-3">
			<aside class="hidden md.block col-12 md.col-3 pl-8 radius-l-md" style="background: #d8e9ff47;">
        <div class="t-0 sticky h-100vh pt-11 pb-13 overflow-auto">

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Alignment
            </div>
            <nav class="flex flex-col">

              {%- for property in alignment -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Appearance
            </div>
            <nav class="flex flex-col">

              {%- for property in appearance -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Background
            </div>
            <nav class="flex flex-col">

              {%- for property in background -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Border
            </div>
            <nav class="flex flex-col">

              {%- for property in border -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Composition
            </div>
            <nav class="flex flex-col">

              {%- for property in composition -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>
          

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Overflow
            </div>
            <nav class="flex flex-col">

              {%- for property in overflow -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Effect
            </div>
            <nav class="flex flex-col">

              {%- for property in effect -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Sizing
            </div>
            <nav class="flex flex-col">

              {%- for property in sizing -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Position
            </div>
            <nav class="flex flex-col">

              {%- for property in position -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Margin
            </div>
            <nav class="flex flex-col">

              {%- for property in margin -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Padding
            </div>
            <nav class="flex flex-col">

              {%- for property in padding -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Typography
            </div>
            <nav class="flex flex-col">

              {%- for property in typography -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Grid
            </div>
            <nav class="flex flex-col">

              {%- for property in grid -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Flexbox
            </div>
            <nav class="flex flex-col">

              {%- for property in flexbox -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Gap
            </div>
            <nav class="flex flex-col">

              {%- for property in gap -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Multi-Column
            </div>
            <nav class="flex flex-col">

              {%- for property in multi-column -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Transform
            </div>
            <nav class="flex flex-col">

              {%- for property in transform -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Transition
            </div>
            <nav class="flex flex-col">

              {%- for property in transition -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              Interactive
            </div>
            <nav class="flex flex-col">

              {%- for property in interactive -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>

          <div class="mb-10 last.mb-0">
            <div class="font-sm font-600 color-gray-900 mb-4">
              SVG
            </div>
            <nav class="flex flex-col">

              {%- for property in svg -%}
                <a href="#{{ property.title }}" class="font-sm xl.font-sm transition-appearance transform translate-x-0 color-gray-200 hover.translate-x-1 leading-9 hover.color-gray-900">{{ property.title }}</a>
              {%- endfor -%}

            </nav>
          </div>



        </div>
      </aside>


			<div class="col-12 col-12 md.col-9 px-19 py-10 pb-19 markdown">

				{%- for property in alignment -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in appearance -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in background -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in border -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in composition -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in overflow -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in sizing -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in position -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in margin -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in padding -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in typography -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in grid -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in flexbox -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in gap -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in multi-column -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in transform -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in transition -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in interactive -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}

        {%- for property in svg -%}
          <article class="mb-8 last.mb-0">
            <div class="markdown">
              <h5 id="{{ property.title | safe }}" class="flex flex-col sm.flex-row align-items-center justify-content-between font-lg md.font-2xl font-600 mb-6 sm.mb-4 pt-7 sm.pt-11">
                <div class="mb-4 sm.mb-0">{{ property.title | safe }}<a href="#{{ property.title | safe }}" class="hidden ml-2 font-500 color-orange-500">#</a> </div>

                {% if property.responsive == true%}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-green-500 tracking-1">
                    <i class="fas fa-check-circle mr-1"></i> 
                    RESPONSIVE
                  </div>
                {% else %}
                  <div class="whitespace-no-wrap font-xs font-600 tracking-1 color-gray-300 tracking-1">
                    <i class="fas fa-times-circle mr-1"></i> 
                    NOT RESPONSIVE
                  </div>
                {% endif %}
              </h5>

              <div class="h-px bg-silver-200 mb-6 sm.mb-3"></div>

              <div class="mb-5 flex align-items-center">
                <div class="mr-2 font-500 font-xs color-gray-100 tracking-1">
                  PSEUDOS
                </div>
                {%- for pseudo in property.pseudos -%}
                  {% if pseudo == "none" %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-silver-300 color-gray-200 mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% else %}
                    <div class="inline-flex px-2 py-1 radius-xs bg-blue-500 color-white mr-1 font-600 font-xs">
                      {{ pseudo }}
                    </div>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>

            <div class="font-sm md.font-md markdown">
              {{ property.description | markdownify | safe }}
            </div>

            <table class="table shadow-1 mt-6">
              <thead class="font-xs font-600 uppercase tracking-1">
                <tr>
                  <th class="w-5-12 md.w-4-12">Class</th>
                  <th>Properties</th>
                </tr>
              </thead>
              <tbody class="font-sm">
                {%- for class in property.classes -%}
                  <tr>
                    <td class="font-xs md.font-sm color-teal-700">
                      {{ class.class | markdownify | safe }}
                    </td>
                    <td class="font-xs md.font-sm color-gray-200">
                      {%- for property in class.properties -%}
                        {{ property | markdownify | safe }}
                      {%- endfor -%}
                    </td>
                  </tr>
                {%- endfor -%}
              </tbody>
            </table>
          </article>
        {%- endfor -%}
			</div>
		</div>
	</div>
</section>